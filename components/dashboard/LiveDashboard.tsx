"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import DashboardShell from "@/components/layout/DashboardShell";

type Role="admin"|"manager"|"employee";
type Data={stats:{todaySales:number|string;todayTransactions:number;count:number;units:number};lowStock:{id:string;name:string;stockQuantity:number}[];recent:{receiptNo:string;amount:number|string;payment:string;status:string;createdAt:string;employeeName:string}[];activity:{action:string;entity:string;createdAt:string;userName:string}[]};
export default function LiveDashboard({role,user,title}:{role:Role;user:string;title:string}){const [data,setData]=useState<Data|null>(null);const [error,setError]=useState("");useEffect(()=>{fetch("/api/dashboard").then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to load dashboard");setData(d)}).catch(e=>setError(e.message))},[]);const money=(v:number|string)=>`₦${Number(v).toLocaleString()}`;return <DashboardShell role={role} user={user} title={title}><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-4"><Metric title="Today's Sales" value={money(data?.stats.todaySales||0)}/><Metric title="Transactions" value={String(data?.stats.todayTransactions||0)}/><Metric title="Products" value={String(data?.stats.count||0)}/><Metric title="Stock Units" value={String(data?.stats.units||0)}/></div>{error&&<p className="mt-5 rounded-xl bg-red-500/10 p-3 text-sm text-red-300">{error}</p>}<div className="mt-5 grid gap-5 lg:grid-cols-2"><Section title={role==="employee"?"Alerts":"Store Health"}>{data?.lowStock.length?<div className="space-y-2">{data.lowStock.map(p=><div key={p.id} className="flex justify-between rounded-lg bg-orange-500/5 p-3 text-sm"><span>{p.name}</span><span className="text-orange-400">{p.stockQuantity} left</span></div>)}</div>:<p className="text-sm text-white/40">No low-stock alerts.</p>}</Section><Section title="Quick Actions"><div className="flex flex-wrap gap-3"><Link href="/sales" className="rounded-xl bg-orange-500 px-4 py-3 text-sm font-semibold text-black">+ Add Sale</Link>{role!=="employee"&&<Link href="/purchases" className="rounded-xl bg-white/10 px-4 py-3 text-sm">+ Add Purchase</Link>}{role==="admin"&&<Link href="/users" className="rounded-xl bg-white/10 px-4 py-3 text-sm">+ Add User</Link>}</div></Section><Section title="Recent Transactions" className="lg:col-span-2"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="text-xs text-white/40"><tr>{["DATE","REF","AMOUNT","PAYMENT","STATUS","STAFF"].map(h=><th key={h} className="px-3 py-3">{h}</th>)}</tr></thead><tbody>{data?.recent.length?data.recent.map(r=><tr key={r.receiptNo} className="border-t border-white/5"><td className="px-3 py-3">{new Date(r.createdAt).toLocaleString()}</td><td className="px-3 py-3">{r.receiptNo}</td><td className="px-3 py-3">{money(r.amount)}</td><td className="px-3 py-3">{r.payment}</td><td className="px-3 py-3">{r.status}</td><td className="px-3 py-3">{r.employeeName}</td></tr>):<tr><td colSpan={6} className="px-3 py-10 text-center text-white/30">No transactions yet.</td></tr>}</tbody></table></div></Section>{role!=="employee"&&<Section title="Activity Feed"><div className="space-y-2">{data?.activity.length?data.activity.map((a,i)=><div key={`${a.createdAt}-${i}`} className="flex justify-between rounded-lg bg-white/5 p-3 text-sm"><span>{a.userName} · {a.action} {a.entity.toLowerCase()}</span><span className="text-white/30">{new Date(a.createdAt).toLocaleString()}</span></div>):<p className="text-sm text-white/40">No activity yet.</p>}</div></Section>}</div></DashboardShell>}
function Metric({title,value}:{title:string;value:string}){return <div className="rounded-2xl border border-white/10 bg-[#151515] p-5"><p className="text-xs text-white/40">{title}</p><p className="mt-2 text-2xl font-semibold">{value}</p></div>}
function Section({title,children,className=""}:{title:string;children:React.ReactNode;className?:string}){return <section className={`rounded-2xl border border-white/10 bg-[#151515] p-5 ${className}`}><h2 className="mb-4 font-semibold">{title}</h2>{children}</section>}
