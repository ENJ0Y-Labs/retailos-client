"use client";
import { FormEvent, useEffect, useState } from "react";
type User={id:string;username:string;fullName:string;role:string;active:boolean;createdAt:string};
export default function UsersPage(){const [users,setUsers]=useState<User[]>([]);const [form,setForm]=useState({username:"",fullName:"",password:"",role:"EMPLOYEE"});const [message,setMessage]=useState("");const [loading,setLoading]=useState(true);async function load(){setLoading(true);try{const r=await fetch("/api/users");const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to load users");setUsers(d.users)}catch(e){setMessage(e instanceof Error?e.message:"Unable to load users")}finally{setLoading(false)}}useEffect(()=>{load()},[]);async function create(e:FormEvent){e.preventDefault();const r=await fetch("/api/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();if(!r.ok){setMessage(d.error||"Unable to create user");return}setMessage("User created.");setForm({username:"",fullName:"",password:"",role:"EMPLOYEE"});load()}async function update(id:string,body:Record<string,unknown>){const r=await fetch("/api/users",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,...body})});const d=await r.json();if(!r.ok)setMessage(d.error||"Unable to update user");else{setMessage("User updated.");load()}}return <main className="min-h-screen bg-[#0b0b0b] p-5 text-white md:p-8"><div className="mx-auto max-w-6xl"><h1 className="text-2xl font-semibold">User Management</h1>{message&&<p className="mt-4 rounded-xl bg-white/5 p-3 text-sm text-orange-300">{message}</p>}<form onSubmit={create} className="mt-6 grid gap-3 rounded-2xl border border-white/10 bg-[#151515] p-5 md:grid-cols-4"><input required value={form.username} onChange={e=>setForm({...form,username:e.target.value})} placeholder="Username" className="field"/><input required value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})} placeholder="Full name" className="field"/><input required minLength={8} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Temporary password" type="password" className="field"/><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})} className="field"><option>EMPLOYEE</option><option>MANAGER</option><option>ADMIN</option></select><button className="rounded-xl bg-orange-500 px-4 py-3 font-semibold text-black md:col-span-4">+ Create User</button></form><div className="mt-6 overflow-x-auto rounded-2xl border border-white/10 bg-[#151515] p-5"><table className="w-full text-left text-sm"><thead className="text-xs text-white/40"><tr>{["USER","USERNAME","ROLE","STATUS","CREATED","ACTION"].map(x=><th key={x} className="px-3 py-3">{x}</th>)}</tr></thead><tbody>{loading?<tr><td colSpan={6} className="px-3 py-10 text-center text-white/30">Loading...</td></tr>:users.map(u=><tr key={u.id} className="border-t border-white/5"><td className="px-3 py-4">{u.fullName}</td><td className="px-3 py-4">{u.username}</td><td className="px-3 py-4"><select value={u.role} onChange={e=>update(u.id,{role:e.target.value})} className="rounded bg-white/5 px-2 py-1"><option>EMPLOYEE</option><option>MANAGER</option><option>ADMIN</option></select></td><td className="px-3 py-4">{u.active?"Active":"Inactive"}</td><td className="px-3 py-4">{new Date(u.createdAt).toLocaleDateString()}</td><td className="px-3 py-4"><button onClick={()=>update(u.id,{active:!u.active})} className={u.active?"text-red-400":"text-green-400"}>{u.active?"Deactivate":"Activate"}</button></td></tr>)}</tbody></table></div></div></main>}
